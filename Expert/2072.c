#include <stdio.h>

void clear_buf();
int main(){
	int TC=0;
    int num=0;
    int total=0;
    scanf("%d",&TC);
    clear_buf();
    for(int i=0; i<TC;i++){

            if()
        printf("#%d %d\n",i,total);
        total=0;
    
    }
}

void clear_buf(){
    while(getchar()!=10);

}